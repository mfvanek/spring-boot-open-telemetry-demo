server:
    port: 8080

opentracing:
    jaeger:
        service-name: mfvanekSpringDemoApp
        enabled: true
        udp-sender:
            host: localhost
            port: 6831

management:
    endpoints:
        web:
            exposure:
                include: "*"

---

opentracing:
    jaeger:
        http-sender:
            url: http://localhost:14268/api/traces
spring:
    config:
        activate:
            on-profile: docker
